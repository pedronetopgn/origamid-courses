var nome = 'Pedro'

var nomeMinusculo = nome.toLowerCase();

var btn = document.querySelector('.btn');

// nomeie 3 propriedades ou métodos de strings
nome.toUpperCase();
nome.indexOf();
nome.length;
// nomeie 5 propriedades ou métodos de elementos da DOM
btn.classList;
btn.addEventListener('click', function() {});
btn.ATTRIBUTE_NODE;
btn.ELEMENT_NODE;
console.log(btn.textContent);

// busque na web um objeto (método) capaz de interagir com o clipboard (parte do computador que lida com o copiar e colar)